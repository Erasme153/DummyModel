# Run records

Each run uses:

`runs/<ladder_version>/<model_size>/<run_id>/`

The run directory should contain the resolved configuration, environment and
dependency snapshot, random seeds, logs, and tracker metadata. It must not be
used as the only location for final checkpoints.

