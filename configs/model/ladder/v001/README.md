# Ladder v001

The files in this directory reserve the initial target sizes. Concrete
architecture fields will be filled only after vocabulary size, weight tying,
context length, and the parameter-count convention have been fixed.

Create `../v002/` instead of editing this ladder after comparable training runs
have begun.

