schema_version: 1
model_id: dummym-v001-p039m
target_parameters: 39000000
status: ready

# 完整架构字段，pretrain.py 直接传入 MiniLlamaConfig，不使用递归 YAML 继承。
# 共享 Embedding / LM Head 后实际参数量为 38,937,088；39M 为近似教学名称。
model_config:
  vocab_size: 32000
  hidden_size: 512
  intermediate_size: 1408
  num_hidden_layers: 8
  num_attention_heads: 8
  num_key_value_heads: 2
  max_position_embeddings: 2048
  rope_theta: 10000.0
  rms_norm_eps: 1.0e-5
  attention_dropout: 0.0
  initializer_range: 0.02
  tie_word_embeddings: true
  pad_token_id: null
  bos_token_id: 1
  eos_token_id: 2
