# Generated artifacts

Use the hierarchy `<ladder_version>/<model_size>/<run_id>/` for checkpoints,
exports, evaluations, profiles, and scaling outputs. Generated binary artifacts
are not committed.

